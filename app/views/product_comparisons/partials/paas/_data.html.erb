<p class="image-icon">
  <%= number_with_delimiter CsAttributeWeighting.calc_paas_credits(cs) %>
  <%= image_tag 'icons/glyphicon-coins.png' %>
</p><hr>

<% @paas_categories.each do |pc| %>
    <% if has_paas_category?(cs, pc) %>

      <p>
        <i class="glyphicon glyphicon-ok text-info"></i>
      </p>
      <hr>

    <% else %>

        <p>
          <i class="glyphicon glyphicon-remove text-danger"></i>
        </p>
        <hr>

    <% end %>
<% end %>

<p><%= number_to_currency cs.price %></p><hr>