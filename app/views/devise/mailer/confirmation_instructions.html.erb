<p><%= t('devise.confirmations.email.welcome', email: @email) %></p>

<p><%= t('devise.confirmations.email.confirm_below') %></p>

<p><%= link_to t('devise.confirmations.email.activate_my_account'), confirmation_url(@resource, confirmation_token: @token) %></p>
