<div class="row">
  <div class="col-xs-12">

    <% flash.each do |type, message| %>

        <div class="alert alert-<%= get_bootstrap_alert_class(type) %>" role="alert">
          <a href="#" data-dismiss="alert" class="close">×</a>

          <ul>
            <li>
              <%= message %>
            </li>
          </ul>
        </div>
    <% end %>

  </div>
</div>

